services:
  - type: web
    name: agriconnect-backend-flask
    runtime: python
    plan: free

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13

    buildCommand: |
      python -m pip install --upgrade pip
      pip install -r requirements.txt

    startCommand: |
      gunicorn app:app